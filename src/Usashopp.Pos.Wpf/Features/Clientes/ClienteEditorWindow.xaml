<Window x:Class="Usashopp.Pos.Wpf.Features.Clientes.ClienteEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:common="clr-namespace:Usashopp.Pos.Wpf.Common"
        xmlns:c="clr-namespace:Usashopp.Pos.Wpf.Features.Clientes"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance c:ClienteEditorViewModel}"
        Title="{Binding Titulo}" SizeToContent="Height" Width="440" WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize" common:VentanaHelpers.Redimensionable="True" FontFamily="{StaticResource Font.Ui}" Background="{StaticResource Brush.Bg.App}">
    <StackPanel Margin="24">
        <TextBlock Text="{Binding Titulo}" FontSize="{StaticResource Font.Title}" FontWeight="SemiBold" Margin="0,0,0,16" />

        <TextBlock Text="Nombre" Foreground="{StaticResource Brush.Text.Secondary}" Margin="0,0,0,4" />
        <TextBox Style="{StaticResource TextBox.Base}" Text="{Binding Nombre, UpdateSourceTrigger=PropertyChanged}" />

        <TextBlock Text="Teléfono" Foreground="{StaticResource Brush.Text.Secondary}" Margin="0,12,0,4" />
        <TextBox Style="{StaticResource TextBox.Base}" Text="{Binding Telefono}" />

        <TextBlock Text="Email" Foreground="{StaticResource Brush.Text.Secondary}" Margin="0,12,0,4" />
        <TextBox Style="{StaticResource TextBox.Base}" Text="{Binding Email}" />

        <TextBlock Text="Notas" Foreground="{StaticResource Brush.Text.Secondary}" Margin="0,12,0,4" />
        <TextBox Style="{StaticResource TextBox.Base}" Text="{Binding Notas}" />

        <TextBlock Text="Límite de crédito" Foreground="{StaticResource Brush.Text.Secondary}" Margin="0,12,0,4" />
        <TextBox Style="{StaticResource TextBox.Base}"
                 Text="{Binding LimiteCredito, UpdateSourceTrigger=PropertyChanged, StringFormat=N2}" />

        <Border BorderBrush="{StaticResource Brush.Border}" BorderThickness="0,1,0,0" Margin="0,16,0,0" Padding="0,12,0,0">
            <StackPanel>
                <TextBlock Text="Datos fiscales (CFDI)" FontWeight="SemiBold" Margin="0,0,0,6" />
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="12" />
                        <ColumnDefinition Width="2*" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="RFC" Foreground="{StaticResource Brush.Text.Secondary}" Margin="0,0,0,4" />
                        <TextBox Style="{StaticResource TextBox.Base}" Text="{Binding Rfc}" />
                    </StackPanel>
                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Razón social" Foreground="{StaticResource Brush.Text.Secondary}" Margin="0,0,0,4" />
                        <TextBox Style="{StaticResource TextBox.Base}" Text="{Binding RazonSocial}" />
                    </StackPanel>
                </Grid>
                <Grid Margin="0,10,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="12" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Régimen fiscal" Foreground="{StaticResource Brush.Text.Secondary}" Margin="0,0,0,4" />
                        <TextBox Style="{StaticResource TextBox.Base}" Text="{Binding RegimenFiscal}" />
                    </StackPanel>
                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Uso CFDI" Foreground="{StaticResource Brush.Text.Secondary}" Margin="0,0,0,4" />
                        <TextBox Style="{StaticResource TextBox.Base}" Text="{Binding UsoCfdi}" />
                    </StackPanel>
                </Grid>
                <TextBlock Text="Dirección fiscal" Foreground="{StaticResource Brush.Text.Secondary}" Margin="0,10,0,4" />
                <TextBox Style="{StaticResource TextBox.Base}" Text="{Binding DireccionFiscal}" />
            </StackPanel>
        </Border>

        <TextBlock Text="{Binding Error}" Foreground="{StaticResource Brush.Critical}" Margin="0,10,0,0"
                   TextWrapping="Wrap" Visibility="{Binding Error, Converter={StaticResource NullToCollapsed}}" />

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Cancelar" Style="{StaticResource Button.Secondary}"
                    Command="{Binding CancelarCommand}" Margin="0,0,10,0" MinWidth="110" />
            <Button Content="Guardar" Style="{StaticResource Button.Primary}"
                    Command="{Binding GuardarCommand}" MinWidth="130" />
        </StackPanel>
    </StackPanel>
</Window>
