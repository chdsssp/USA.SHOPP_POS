<Window x:Class="Usashopp.Pos.Wpf.Features.Roles.RolEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:common="clr-namespace:Usashopp.Pos.Wpf.Common"
        xmlns:r="clr-namespace:Usashopp.Pos.Wpf.Features.Roles"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance r:RolEditorViewModel}"
        Title="{Binding Titulo}" SizeToContent="Height" Width="460" WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize" common:VentanaHelpers.Redimensionable="True" FontFamily="{StaticResource Font.Ui}" Background="{StaticResource Brush.Bg.App}">
    <StackPanel Margin="24">
        <TextBlock Text="{Binding Titulo}" FontSize="{StaticResource Font.Title}" FontWeight="SemiBold" Margin="0,0,0,16" />

        <TextBlock Text="Nombre del rol" Foreground="{StaticResource Brush.Text.Secondary}" Margin="0,0,0,4" />
        <TextBox Style="{StaticResource TextBox.Base}" Text="{Binding Nombre, UpdateSourceTrigger=PropertyChanged}" />

        <TextBlock Text="Permisos" Foreground="{StaticResource Brush.Text.Secondary}" Margin="0,16,0,6" />
        <Border Style="{StaticResource Card}" Padding="12" MaxHeight="320">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Permisos}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <CheckBox Content="{Binding Etiqueta}" IsChecked="{Binding Asignado}"
                                      Margin="0,5" VerticalContentAlignment="Center" />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>

        <TextBlock Text="{Binding Error}" Foreground="{StaticResource Brush.Critical}" Margin="0,12,0,0"
                   TextWrapping="Wrap" Visibility="{Binding Error, Converter={StaticResource NullToCollapsed}}" />

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="Cancelar" Style="{StaticResource Button.Secondary}"
                    Command="{Binding CancelarCommand}" Margin="0,0,10,0" MinWidth="110" />
            <Button Content="Guardar" Style="{StaticResource Button.Primary}"
                    Command="{Binding GuardarCommand}" MinWidth="130"
                    IsEnabled="{Binding Ocupado, Converter={StaticResource InverseBool}}" />
        </StackPanel>
    </StackPanel>
</Window>
