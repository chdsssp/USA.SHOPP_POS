<Window x:Class="Usashopp.Pos.Wpf.Features.Pos.VentasEnEsperaWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:pos="clr-namespace:Usashopp.Pos.Wpf.Features.Pos"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance pos:VentasEnEsperaViewModel}"
        Title="Ventas en espera" Height="480" Width="520" WindowStartupLocation="CenterOwner"
        FontFamily="{StaticResource Font.Ui}" Background="{StaticResource Brush.Bg.App}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="12" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Ventas en espera" FontSize="{StaticResource Font.Title}" FontWeight="SemiBold" />

        <Border Grid.Row="2" Style="{StaticResource Card}" ClipToBounds="True">
            <Grid>
                <TextBlock Text="No hay ventas en espera." VerticalAlignment="Center" HorizontalAlignment="Center"
                           Foreground="{StaticResource Brush.Text.Disabled}"
                           Visibility="{Binding Items.Count, Converter={StaticResource EmptyToVis}}" />
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <ItemsControl ItemsSource="{Binding Items}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border BorderBrush="{StaticResource Brush.Border}" BorderThickness="0,0,0,1" Padding="16,10">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <StackPanel Grid.Column="0" VerticalAlignment="Center">
                                            <TextBlock Text="{Binding Etiqueta}" FontWeight="SemiBold" />
                                            <TextBlock Foreground="{StaticResource Brush.Text.Secondary}" FontSize="12">
                                                <Run Text="{Binding HoraTexto, Mode=OneWay}" /><Run Text=" · " /><Run Text="{Binding Articulos, Mode=OneWay}" /><Run Text=" art. · " /><Run Text="{Binding Total, StringFormat=C2, Mode=OneWay}" />
                                            </TextBlock>
                                        </StackPanel>
                                        <Button Grid.Column="1" Content="Descartar" Style="{StaticResource Button.Subtle}" MinHeight="34"
                                                Padding="10,4" Margin="0,0,8,0" FontSize="12.5" Foreground="{StaticResource Brush.Critical}"
                                                Command="{Binding DataContext.DescartarCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                CommandParameter="{Binding}" />
                                        <Button Grid.Column="2" Content="Recuperar" Style="{StaticResource Button.Primary}" MinHeight="34"
                                                Padding="14,4" FontSize="12.5"
                                                Command="{Binding DataContext.RecuperarCommand, RelativeSource={RelativeSource AncestorType=ItemsControl}}"
                                                CommandParameter="{Binding}" />
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </Border>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Cerrar" Style="{StaticResource Button.Secondary}" MinWidth="120" Command="{Binding CancelarCommand}" />
        </StackPanel>
    </Grid>
</Window>
