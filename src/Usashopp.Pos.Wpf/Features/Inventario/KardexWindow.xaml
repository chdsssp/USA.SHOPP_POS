<Window x:Class="Usashopp.Pos.Wpf.Features.Inventario.KardexWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:inv="clr-namespace:Usashopp.Pos.Wpf.Features.Inventario"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance inv:KardexViewModel}"
        Title="Kardex" Height="560" Width="640" WindowStartupLocation="CenterOwner"
        FontFamily="{StaticResource Font.Ui}" Background="{StaticResource Brush.Bg.App}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <StackPanel HorizontalAlignment="Left">
                <TextBlock Text="{Binding Titulo}" FontSize="{StaticResource Font.Title}" FontWeight="SemiBold" />
                <TextBlock Text="{Binding Subtitulo}" Foreground="{StaticResource Brush.Text.Secondary}"
                           FontSize="12.5" Margin="0,2,0,0" />
            </StackPanel>
            <StackPanel HorizontalAlignment="Right" VerticalAlignment="Center">
                <TextBlock Text="Existencia actual" Foreground="{StaticResource Brush.Text.Secondary}"
                           FontSize="12" HorizontalAlignment="Right" />
                <TextBlock Text="{Binding StockActual}" FontSize="{StaticResource Font.Title}"
                           FontWeight="Bold" HorizontalAlignment="Right" />
            </StackPanel>
        </Grid>

        <!-- Filtros -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,14,0,12">
            <TextBlock Text="Desde" VerticalAlignment="Center" Foreground="{StaticResource Brush.Text.Secondary}" Margin="0,0,8,0" />
            <DatePicker SelectedDate="{Binding Desde}" MinHeight="38" VerticalContentAlignment="Center" Width="140" />
            <TextBlock Text="Hasta" VerticalAlignment="Center" Foreground="{StaticResource Brush.Text.Secondary}" Margin="14,0,8,0" />
            <DatePicker SelectedDate="{Binding Hasta}" MinHeight="38" VerticalContentAlignment="Center" Width="140" />
            <ComboBox SelectedIndex="{Binding FiltroTipo}" MinHeight="38" Width="150" Margin="14,0,0,0" VerticalContentAlignment="Center">
                <ComboBoxItem Content="Todos" />
                <ComboBoxItem Content="Solo entradas" />
                <ComboBoxItem Content="Solo salidas" />
            </ComboBox>
            <Button Content="Exportar CSV" Style="{StaticResource Button.Secondary}" MinHeight="38"
                    Command="{Binding ExportarCsvCommand}" Margin="14,0,0,0" />
        </StackPanel>

        <Border Grid.Row="2" Style="{StaticResource Card}" ClipToBounds="True">
            <Grid>
                <DataGrid ItemsSource="{Binding Movimientos}"
                          AutoGenerateColumns="False" IsReadOnly="True" HeadersVisibility="Column"
                          GridLinesVisibility="Horizontal" BorderThickness="0" RowHeight="40"
                          Background="{StaticResource Brush.Surface}"
                          HorizontalGridLinesBrush="{StaticResource Brush.Border}"
                          FontFamily="{StaticResource Font.Ui}" FontSize="13" SelectionMode="Single">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Fecha" Binding="{Binding Fecha, StringFormat=dd/MM/yyyy HH:mm}" Width="150" />
                        <DataGridTextColumn Header="Movimiento" Binding="{Binding Tipo}" Width="140" />
                        <DataGridTextColumn Header="Cantidad" Binding="{Binding Cantidad}" Width="90">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="HorizontalAlignment" Value="Right" />
                                    <Setter Property="FontWeight" Value="SemiBold" />
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="Saldo" Binding="{Binding Saldo}" Width="80">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="HorizontalAlignment" Value="Right" />
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Header="Motivo" Binding="{Binding Motivo}" Width="*" />
                    </DataGrid.Columns>
                </DataGrid>
                <TextBlock Text="Esta variante aún no tiene movimientos de inventario." VerticalAlignment="Center"
                           HorizontalAlignment="Center" Foreground="{StaticResource Brush.Text.Disabled}"
                           Visibility="{Binding Movimientos.Count, Converter={StaticResource EmptyToVis}}" />
            </Grid>
        </Border>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Cerrar" Style="{StaticResource Button.Secondary}" MinWidth="120" Click="OnCerrar" />
        </StackPanel>
    </Grid>
</Window>
